#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -x /usr/lib/commander/system-agent ]]; then
  exec /usr/lib/commander/system-agent "$@"
fi
exec "$SCRIPT_DIR/system-agent" "$@"

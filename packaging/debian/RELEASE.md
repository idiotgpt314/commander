# Debian Release

Build the package with:

```bash
./packaging/debian/build-deb.sh
```

Attach the resulting `.deb` to the matching GitHub release tag.

Target package name:

- `commander_<version>_amd64.deb`
